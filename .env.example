# Firebase Admin credentials (JSON string)
# IMPORTANT: keep this secret. In Vercel, set as an Environment Variable.
FIREBASE_SERVICE_ACCOUNT_JSON='{ "type":"service_account", "project_id":"...", "private_key":"-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n", "client_email":"..." }'

# Protect MCP endpoint
DIFF_MCP_TOKEN='change-me'

# Optional: if set, UI API endpoints require x-ui-token
# DIFF_UI_TOKEN='ui-token'

# Optional: helps generate resolve URLs
# APP_BASE_URL='https://your-app.vercel.app'

# Optional: UI server-side fetching base
# NEXT_PUBLIC_APP_BASE_URL='https://your-app.vercel.app'
